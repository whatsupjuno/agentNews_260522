{
  "name": "@agentnews/shared",
  "version": "0.1.0",
  "private": true,
  "description": "agentNews 공유 타입·상수·에러 코드 (백엔드/모바일 공통)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint for shared yet' && exit 0",
    "test": "echo 'no tests for shared yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
